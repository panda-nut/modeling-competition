# 进度与模型管理

本目录是项目状态的统一入口，内容使用中文。

读取顺序：

1. `progress-log.md`：只读最近的正式进度记录；
2. `model-map.md`：定位当前问题的模型、输入输出和证据；
3. `history/`：仅在需要追溯旧决策时阅读；
4. `.working/`：本地临时区，不进入 Git。

正式进度只能追加。一个任务中的尝试、撤回和未确认内容先写入 `.working/progress-draft.md`，任务结束时整理成一条确定记录。


# 数学建模团队协作 SOP（Standard Operating Procedure）


## 一、项目初始化

### 1.1 分支结构

```
main                              ← 长期主干，存放通用模板与沉淀的提示词
├── practice/<题目编号或简称>      ← 练习赛分支（日常训练用）
└── contest/<题目编号或简称>       ← 正式比赛分支（比赛期间用）
```

- 每次新题目从 `main` 拉出对应分支，题目结束后合并回 `main`。
- 分支命名统一小写、用短横线分隔，如 `practice/heat-conduction`。

### 1.2 功能模块（位于 `main`）

`main` 分支维护三份**提示词基准文件**，作为交给 AI 的标准输入：

| 文件 | 用途 |
|------|------|
| `建模提示词.md` | 指导 AI 完成模型建立、求解与验证 |
| `代码提示词.md` | 指导 AI 编写、调试与优化代码 |
| `论文写作提示词.md` | 指导 AI 撰写论文各章节 |

**更新规则**：每冻结一份 `practice/` 产出后，回溯总结本轮经验，更新 `main` 中对应的提示词文件。`main` 中的提示词始终是"交给 AI 的最新基准"，务必保证准确、可直接使用。

### 1.3 AI 基本规则（`AGENTS.md`）

在 `main` 中维护一份 `AGENTS.md`，定义：

- AI 的角色边界（能做什么、不能做什么）
- 上下文加载清单（每轮对话开始时 AI 应读取哪些文件）
- 文件上传/交付流程（AI 产出物应放到哪个目录、命名规则是什么）
- **每轮回答需包含固定称呼**（如"各位队友好"），以便人类快速确认 AI 未丢失上下文

---

## 二、工作目录结构

每个题目分支（`practice/<题目>` 或 `contest/<题目>`）按以下标准结构组织：

| 目录 | 用途 | 主要文件 |
|------|------|----------|
| `00_progress/` | 进度追踪与模型总览 | `progress-log.md`（进度日志）、`model-map.md`（模型关系图） |
| `01_problem/` | 官方题面与可读化文本 | `README.md`（题面摘要、关键约束提炼） |
| `02_references/` | 文献、索引与引用管理 | `references-index.md`（文献索引表） |
| `03_data/` | 原始数据与预处理后的 CSV | `README.md`（数据来源说明、字段含义、预处理步骤） |
| `04_code/` | 建模、求解、验证、绘图代码 | `README.md`（各脚本功能说明、运行依赖、调用顺序） |
| `05_figures/` | 工作阶段图表（中间产物） | `README.md`（图表说明、对应代码路径） |
| `06_drafts/` | 每问的演示性 TEX/PDF 草稿 | `README.md`（各草稿对应的问题编号与状态） |
| `07_frozen/` | 经团队确认的冻结快照 | `README.md`（冻结版本号、确认时间、确认人） |
| `08_results/` | 冻结晋升后的正式结果 | `results-index.md`（结果索引、对应冻结版本） |
| `09_submission/` | 最终提交材料 | `submission-checklist.md`（提交清单与完成状态） |

### 数据与产物的流转路径

```
题面 & 文献（01/02）
  → 原始数据（03）
    → 预处理 CSV（03）
      → 建模 / 验证程序（04）
        → 绘图程序读取 CSV → 生成图表（05）
          → 演示性论文草稿（06）
            → 团队复核 ✓
              → 冻结快照（07_frozen）
                → 晋升为正式结果（08_results）
                  → 汇编进提交材料（09_submission）
```

**关键原则**：每个环节只消费上一环节的产物，不跳级引用。发现问题应**回溯到上游角色修复**，而非在当前目录直接修改。

---

## 三、角色与权限

团队分为四个工作角色，每人职责明确、读写范围受限：

| 角色 | 职责 | 可读目录 | 可写目录 |
|------|------|----------|----------|
| **A — 建模手** | 读题、建模型、写代码、出图 | 00–05 全部 | 00、01、02、03、04、05 |
| **B — 质控/归档** | 复核产出、冻结快照 | 03、04、05、06 | 07 |
| **C — 结果整合** | 汇总冻结结果、整理为论文可用素材 | 02、07 | 08 |
| **D — 论文手** | 撰写最终论文与提交材料 | 08 | 09 |

### 权限说明

- **只读 ≠ 不管**：如果只读目录中发现问题，应**反馈给对应角色修改**，不得自行直接改动。
- **A 角色**是唯一的"生产端"，负责从题面到图表的全部技术产出。
- **B 角色**是质量关卡，冻结操作必须经 B 确认。
- **C 角色**将冻结成果结构化，为论文写作提供可直接引用的数据与结论。
- **D 角色**基于 `08_results/` 中的内容完成论文，不直接接触原始代码或中间产物。

---

## 四、日常协作流程

### 4.1 每轮 AI 对话的开启检查

每次与 AI 对话开始时，确保 AI 加载了以下两份基础文件：

1. 当前分支对应的**功能模块提示词**（建模 / 代码 / 论文写作）
2. `AGENTS.md`（角色边界与交付规范）

**确认方式**：AI 每轮回答开头需包含约定称呼（如"各位队友好，我是建模助手"）。若未出现，说明上下文已丢失，需重新加载。

### 4.2 一轮典型工作循环

```
① A 读题，产出建模方案 → 01/04/05
② A 完成代码与图表，提交至对应目录
③ B 复核 A 的产出（03–06），确认无误后冻结 → 07
④ C 将冻结成果整理进 08
⑤ D 基于 08 撰写/更新论文 → 09
⑥ 团队终审 → 更新 09 中的 submission-checklist
```

### 4.3 提示词的迭代闭环

每完成一轮冻结后，A/B/C/D 各自回顾本轮工作，提炼经验：

- 哪些提示词有效？哪些导致 AI 产出偏差？
- 新增的约束、格式要求、常见错误

→ 更新 `main` 中对应的提示词文件，形成**"越用越准"**的良性循环。

---

## 五、归档与收尾

### 5.1 项目结束时的归档动作

1. 创建版本标签（如 `v1.0-2026MCM-A`），标记最终提交版本。
2. 将以下材料合并回 `main`：
   - 最终论文（PDF + TEX 源码）
   - 完整可运行代码（含依赖说明）
   - 数据复现说明（从原始数据到最终结果的完整步骤）
3. 更新 `main` 中的三份提示词文件，纳入本轮经验。

### 5.2 归档禁区

以下内容**不得**并入归档：

- ❌ 本地 AI 对话过程记录
- ❌ 批量试验的中间缓存文件
- ❌ 未筛选、未复核的原始运行结果
- ❌ 任何未经冻结确认的工作产物

---

## 六、命名与格式规范

| 项目 | 规范 |
|------|------|
| 文件名 | 小写字母 + 数字 + 短横线，如 `model-solver-v2.py` |
| 冻结版本 | `frozen-v<序号>-<日期>`，如 `frozen-v3-20260729` |
| 图表文件 | `<问题编号>-<图表类型>-<序号>.png/pdf`，如 `q1-heatmap-01.png` |
| 进度日志 | 按日期追加，格式为 `## YYYY-MM-DD HH:MM` |
| 代码注释 | 每个 `.py` / `.tex` 文件头部注明：作者、对应问题编号、最后修改日期 |

---

## 七、快速检查清单

### 开赛前
- [ ] `main` 中三份提示词已更新到最新版本
- [ ] `AGENTS.md` 已定义角色边界与交付规范
- [ ] 各角色已明确自己的读写范围

### 比赛进行中
- [ ] 每轮 AI 对话已确认上下文加载（检查称呼）
- [ ] 所有产出物已放入对应目录，命名规范
- [ ] 冻结前已通过 B 角色复核
- [ ] 发现上游问题已反馈对应角色，未自行修改

### 赛后归档
- [ ] 版本标签已创建
- [ ] 最终论文、代码、复现说明已合并至 `main`
- [ ] 提示词已根据本轮经验更新
- [ ] 无中间缓存/未筛选结果混入归档



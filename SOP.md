# 团队协作 SOP

## 1. 启动项目

从 `main` 建立 `practice/<题目>` 或 `contest/<题目>` 分支；在 `projects/<项目名>/01_architecture/` 记录题目拆解、假设、指标和负责人。

## 2. 阶段协作

各成员从项目分支创建明确的 `feature/<模块>` 分支。提交前自行运行代码、清理中间文件，并在 Pull Request 中说明问题、方法、输入、输出、复现方式、局限性和需要复核的点。

## 3. 合并与冻结

通过复核后合并至项目分支。最终提交前将版本、随机种子、数据版本、运行环境和图表编号写入 `10_final/README.md`；只将可提交版本标记为 `[FINAL]`。

## 4. 归档

项目结束后创建版本标签，将最终论文、代码和复现说明归档到 `main`。不要把本地 AI 过程、批量试验缓存或未筛选结果并入归档。

# [A] 角色子配置

## 角色登记

- 角色 ID：`[A]`
- 首次使用时由用户确认；本机确认记录仅写入 `00_progress/.working/agent-role.json`。
- 角色职责、可操作路径和远程目标须由当前用户在具体任务中确认；本文件不自行推断团队分工。

## 操作边界

- 本地进度记录、生成文件归属、提交标题和上传计划使用 `[A]` 标记。
- 读取远程 `AGENTS.md`、本文件或其他角色子配置前，必须先取得当前用户对目标文件的明确授权。
- 读取 `[B]agent.md` 或 `[C]agent.md` 前，即使当前角色已确认，也必须逐次取得授权。
- 上传前按根 `AGENTS.md` 第 15.2 节获取目标远程分支快照并比较精确路径；未经当前用户明确授权不得推送。

## 文件与交接

- 新建或实质编辑的团队工作产物在扩展名前使用 `_<A>`；共享治理文件按根规则登记角色和路径，不改名。
- 交接报告写明本次角色 `[A]`、本地提交、远程快照和是否实际推送。

# [A] Agent 角色子规则：初模与草稿交付

> 本文件只规定 `[A]` 在数学建模项目中的角色专属工作。根 `AGENTS.md`、当前用户指令和目录 README 的优先级高于本文件；跨角色共同规则不在此重复定义。

## 1. 启动与边界

- 确认当前角色为 `[A]` 后，先读取根 `AGENTS.md`、本文件和任务相关 README；未获得授权时不得读取其他角色子配置。
- `[A]` 负责题意拆解、方法空间检索、数据接口设计、每小问 V0 初模和解释性草稿，不负责自行宣布复核通过、冻结或正式结果晋升。
- 本机角色映射、MiKTeX 版本/路径、安装状态、终端输出和过程草稿只保存在本地忽略区，不写入受版本管理文件、提交正文或远程日志。

## 2. 建模与 V0 交付

每一小问的 V0 必须至少提供：

- 问题目标、数学表达、变量、参数、单位、假设和适用范围；
- 主模型、备用模型和最简基线，以及选择理由；
- 可重建的数据接口、处理程序、模型入口和受控随机种子；
- 初步 CSV、图表、解释性 PDF、Evidence ID 和固定源 commit；
- 已知限制、资料缺口、待验证问题和可能影响下游的风险。

V0 只能作为候选输出。不得把 V0 数字、图表或结论称为最终、最优、已验证或正式结果；未通过 B 复核的内容不得交给 C 作为正式论文数字。

## 3. 方法与资料职责

- 使用任务检索包搜索适用模型、公式、算法、参数估计、求解器、替代方案和最简基线。
- 仅使用已核验资料支持核心模型选择、关键假设和参数；未读全文资料只能标记为候选，不得写成已证实结论。
- 为每个关键判断登记 Evidence ID、来源、适用范围和风险；资料检索过程、聊天记录和本机下载位置不得上传。

## 4. 数据接口与可重复运行

- 明确字段、单位、时间尺度、空间尺度、合法域、缺失值和异常值规则；原始数据保持只读。
- 重要预处理、建模、验证和绘图输入必须输出为可重建的 CSV；不得手工修改正式处理数据。
- 记录受控复现入口、输入哈希、随机种子和关键产物哈希；不得上传本机命令历史、环境审计或终端日志。
- A 的模型代码不得修改冻结层、结果层或提交层文件；图表生成不得重新拟合模型或暗中改写正式数字。

## 5. Draft 与 LaTeX 交付

- 草稿只放在 `06_drafts/q*/`。每个实质技术阶段或经确认修订均建立对应 `.tex`，并保留由该 TEX 编译得到的同名 PDF。
- 阶段标识使用仓库定义的 `v0`、`v1`、`draft` 等；不使用 `final`、`latest`、`最终版2` 等模糊版本词。细小修改由 Git commit 追溯，不机械复制文件。
- 使用本机可用的 MiKTeX 工具链编译；实际版本和本机配置仅在本地读取和记录，不能进入远程仓库。
- 只编辑 TEX，PDF 必须重新编译生成；编译成功后确认 PDF 存在、与当前 TEX 对应，并检查引用、图表和公式是否能正常呈现。
- 辅助文件、日志和缓存只写入 `06_drafts/q*/.latex-build/<草稿标识>/`；该目录必须由 `.gitignore` 忽略。不得提交 `.aux`、`.log`、`.out`、`.synctex*` 或其他构建过程文件。
- Draft 中只能引用已通过 `review=pass` 或 `review=pass_with_limits` 的内容作为正式结论；限制必须紧邻相关结果说明。

## 6. 与 B、C 的交接

- 向 B 交接时提供固定 commit、数据/代码入口、接口说明、初步结果、假设、风险、待验证清单和 Evidence ID；B 的复核必须基于该固定版本。
- B 发现 P0 或 `reject` 时，优先修复受影响上游内容，暂停依赖该结果的下游传播；修复后重新交接和复核。
- 等待 B 复核期间，可推进不依赖未确认结果的下一小问资料、数据、接口、基线或代码框架。
- 向 C 提供的内容必须标明阶段与复核结论；只有冻结并晋升的结果可作为正式论文核心数字。

## 7. 文件、提交与远程操作

- 新建或实质编辑的工作产物遵守根规则的角色标记、目录、命名和精确暂存要求；共享治理文件不因角色而改名。
- 提交前执行与修改相符的验证和 `git diff --check`，只暂存本次精确路径。
- 远程推送前按根规则获取目标快照并比较差异；未经当前用户明确授权不得推送。发现冲突时展示差异和时间戳，不得覆盖他人工作。
- 交接只报告任务摘要、角色标记、修改路径、验证、提交、快照和是否推送；不记录本机身份或原始过程信息。

# 数学建模三人协作工作流参考

> 本文件是供各角色 Agent 在不同阶段协作时参考的工作流说明，不替代根 `AGENTS.md`、角色子配置或当前用户指令。规则冲突时仍按根 `AGENTS.md` 的优先级处理。
>
> 文中的 `[A]`、`[B]`、`[C]` 为抽象协作角色。赛题 A、赛题 B 与角色标签无关。

## 1. 总体原则

- 采用“逐题滚动、阶段门控、固定提交交接”的方式，避免 A 完成全部工作后 B 才开始复核，也避免 C 等待所有小问才开始论文工作。
- 每个结论都必须区分 `v0`、`v1`、`draft`、`frozen`；未经复核的内容不得作为正式结果或论文正式数字。
- 原始数据保持只读；处理数据、模型、验证和图表必须可由受版本管理的程序重建。
- 远程仓库只保存可复核的交付物、摘要、证据和决策；不得上传本机环境信息、原始终端输出、聊天记录或过程草稿。
- 角色间以固定 commit、文件哈希、Evidence ID 和结构化交接内容沟通，不以聊天中的未验证结论替代文件证据。

## 2. 阶段 1：选题与题目分析

比赛开始后，A、B 均独立阅读赛题 A 和赛题 B；A 从建模可行性出发，B 从现实与验证可行性出发形成判断。选题阶段使用结构化选题卡和评分表，不要求生成解释性 PDF；解释性 PDF 仅属于后续 V0 交付。

### `[A]` 的输出

题目如何被转化为可计算、可解释、可交付的模型？

- 每个小问的输入、输出和交付形式；
- 小问之间的依赖关系；
- 目标函数、约束、决策变量、状态变量和单位；
- 已知量、待估计量及其可能来源；
- 主模型、备用模型和最简基线；
- 计算量、求解难点和失败风险。

### `[B]` 的输出

该模型所依赖的数据、假设、约束与结果是否真实、可靠、可验证？

- 数据完整性、单位、口径和可用性；
- 题目隐含条件及现实中不可忽略的约束；
- 各小问的验证方式和可获得的外部对照；
- 数据泄漏、外推、不可识别和评价失真的风险；
- 两道赛题的资料可获得性、验证成本和总体风险。

### 阶段交付与门控

- AB 共同形成选题评分表，至少比较题意清晰度、数据可用性、模型可解释性、验证可行性、计算成本、资料风险和论文叙事完整性。
- 每道候选赛题均应有一页选题卡，汇总 A 的模型路线、B 的数据/验证风险和待确认事项。
- 用户确认最终赛题后，才进入正式建模；未选题的分析仅保留为工作材料，不得混入正式结果。

## 3. 阶段 2：资料检索与现实背景核验

资料检索与题目分析、数据理解并行进行，而不是在模型完成后补做。Agent 负责扩展候选、整理线索和辅助定位；原文、官方页面和权威数据源才是正式证据。

### 3.1 检索包

每一题开始检索前，先形成一页检索包，而不是只向 Agent 粘贴完整题干。检索包至少包含：

- 研究对象及空间、时间范围；
- 每个小问的目标、输入和输出；
- 已有数据字段、单位、样本量、时间范围和缺失情况；
- 可能涉及的学科术语；
- 明确不确定的概念、待查参数和待验证结论；
- 题面已给定的边界，以及不得自行假设的边界。

检索词应由泛泛的“某问题数学建模”收敛为“对象 + 任务 + 方法 + 约束 + 指标”。检索包是 A、B 后续检索和讨论的共同输入；题意、数据或约束变化时必须同步更新。

### 3.2 `[A]`：方法空间检索

`[A]` 使用检索包让 Agent 搜索：

- 可用于解题的数学模型、经典公式、算法和数值方法；
- 同类问题的建模结构和常见假设；
- 参数估计、求解器和复杂度控制方法；
- 主模型的可替代方案和最简基线。

建议提示词：

> 基于以下题干和数据结构，列出适合的数学模型、经典算法和可替代基线。每项说明适用条件、输入输出、假设、计算复杂度、常见失败模式，并提供可核验的论文题名、作者、年份、DOI 或原文链接。不要把搜索摘要当作论文结论。

### 3.3 `[B]`：现实与验证空间检索

`[B]` 使用检索包让 Agent 搜索：

- 行业标准、政府数据、官方统计和技术规范；
- 参数现实范围、典型数量级和评价基线；
- 安全、资源、物理或管理约束；
- 可用于外部验证的数据和研究。

建议提示词：

> 基于以下题干和数据结构，检索官方统计、行业标准、技术规范、参数范围、评价指标和可用于外部验证的数据。优先政府、标准组织、原始数据发布机构和同行评审原文；每项给出发布机构、年份、链接、适用范围及可能局限。

这种分工使 A 避免只选择“看起来高级”的模型，也使 B 不只积累背景资料而缺少可执行的验证依据。

### 3.4 候选资料与已核实文献分层

Agent 找到的论文、网页和数据源先进入候选池，不得直接作为模型假设、参数来源或论文引用。每篇候选资料至少核对：

- 题名、作者、年份、期刊或机构、DOI/URL 是否真实；
- 是否能打开原文、正式出版页或官方发布页；
- 当前阅读范围是全文、部分内容还是仅摘要；
- 它支持的是哪一个具体结论；
- 结论的对象、时间、数据条件是否与赛题一致。

未读全文的资料必须标记为“摘要级候选”，不得据此声称公式、参数或结论已被证实。只有核验后的资料才能登记入正式文献索引和 `references.bib`。

### 3.5 证据卡片

每一条实际用于模型或论文的资料形成一张简短证据卡片，而不是仅堆积链接：

| 字段 | 内容 |
| --- | --- |
| Evidence ID | 例如 `E-Q2-004` |
| 来源 | DOI、官方网页或数据发布页 |
| 支持结论 | 一句可检验的结论 |
| 适用范围 | 数据、对象、时间或条件 |
| 使用位置 | 模型假设、参数、验证指标或论文段落 |
| 核验状态 | 全文已读 / 部分已读 / 摘要级候选 |
| 风险 | 外推、样本差异、版本过旧等 |

证据卡片使 B 能快速核对 A 的模型假设，也让 C 能从已核实来源中引用，不必重复检索。

### 3.6 漏斗式多轮检索

```text
第 1 轮：宽搜领域术语、常见方法、权威机构和综述
→ 第 2 轮：精搜“对象 + 指标 + 方法/约束”，定位原始论文和标准
→ 第 3 轮：反向追踪高质量文献的参考文献、被引文献和数据源
→ 第 4 轮：验证搜反例、失败条件、不同结论和方法局限
```

第四轮用于主动寻找不支持当前方案的证据，防止 Agent 或团队只收集支持既有模型的资料。

### 3.7 停止规则与资料使用边界

资料检索满足以下条件即可停止扩张并进入建模或复核：

- 每个核心模型至少有一个可信来源；
- 每个关键参数或约束有可追溯依据；
- 每个评价指标有定义或参考；
- 至少找到一种替代模型、反例或失败条件；
- B 已明确资料缺口和外推边界。

找不到可靠来源时，相关内容必须降级为假设或情景设定，不能包装成事实。

### 3.8 远程保存边界

远程仓库只保存已核实的文献条目、引用信息、证据卡片、资料结论和来源说明。浏览过程、聊天记录、原始搜索痕迹、终端输出和本机下载路径只保存在本地忽略区，不得上传。

资料检索的阶段交付为：检索包、候选池、已核实文献索引、证据卡片，以及资料风险和缺口清单。

## 4. 阶段 3：建立 V0 初步模型

每一小问由 `[A]` 先建立可复现的数据接口，再提交一个完整但未冻结的 V0 包。

### 数据接口与预处理

- 明确字段、单位、时间尺度、空间尺度、缺失值和异常值规则；
- 编写可重复执行的预处理程序，输出规范 CSV；
- 原始数据保持只读，任何处理数据必须可由程序重建；
- 向 `[B]` 说明关键字段、数据质量风险和模型所需参数，供其独立审计。

### V0 交付包

- 问题目标、数学表达、变量、参数、单位、假设和适用范围；
- 主模型、备用模型和最简基线；
- 可复现的代码入口、受控配置和随机种子；
- 初步结果 CSV、图表和解释性 PDF；
- 已知限制、待验证问题、Evidence ID 和固定源 commit。

解释性 PDF 必须说明：模型选择理由、模型与题意的对应关系、数据如何进入模型、初步结果的含义，以及尚未验证的结论。

V0 只能标记为 `progress=in_progress`、`stage=v0`、`review=pending`，不得被称为最终模型或正式结果。

## 5. 阶段 4：B 的独立复核与模型增强

`[B]` 在收到 A 的固定 commit 后，以独立复现为基础开展检查，不直接在 A 的文件上修补。

- 是否可按登记入口独立运行；
- 原始数据哈希、完整性、重复值、缺失值和异常值是否已核对；
- 字段单位换算、时间/空间对齐、分布和共线性是否合理；
- 训练、验证、测试切分是否存在数据泄漏，预处理是否人为改善结果；
- 公式、索引、单位、目标方向和约束是否正确；
- 是否优于最简基线，且结果是否符合现实数量级；
- 参数是否可识别，是否存在过拟合、泄漏或外推；
- 随机算法是否收敛，结果对参数和数据扰动是否敏感；
- 是否存在更合理的模型、算法、约束或求解策略。

复核输出使用以下结论：

- `reject`：发现 P0 问题，必须退回 A 修复；
- `pass_with_limits`：可用，但限制和适用范围必须进入结果与论文；
- `pass`：满足当前复核标准，可进入冻结候选。

## 6. 阶段 5：AB 讨论与冲突解决

讨论围绕可复核证据，不围绕模型名称或角色权威。每次关键讨论形成简短决策摘要，至少比较：

| 比较项 | A 模型 | B 建议 | 采用决定 |
| --- | --- | --- | --- |
| 题意符合度 |  |  |  |
| 现实合理性 |  |  |  |
| 预测或拟合误差 |  |  |  |
| 约束满足 |  |  |  |
| 稳健性 |  |  |  |
| 可解释性 |  |  |  |
| 计算成本 |  |  |  |

发生分歧时，双方必须提供对应代码、数据、结果和 Evidence；无法确定时保留主模型与备用模型，由用户决定。禁止直接覆盖对方文件；只上传决策摘要，不上传原始讨论过程。

## 7. 阶段 6：滚动推进与等待边界

| 轮次 | `[A]` | `[B]` | `[C]` |
| --- | --- | --- | --- |
| 第 1 轮 | Q1 初模 | 全局数据与文献核验 | 背景、问题重述、符号表 |
| 第 2 轮 | Q2 初模 | Q1 复核 | Q1 方法骨架 |
| 第 3 轮 | Q3 初模或修复 Q1 | Q2 复核 | 写已确认的 Q1 内容 |
| 第 4 轮 | 后续小问与回归修复 | Q3 复核及整体回归 | 写已冻结部分 |

等待规则：

- A 不必等待 B 完成前一问，允许推进下一问中不依赖前问正式结果的资料、数据、接口、基线和代码框架。
- 未通过 B 复核的数字不得交给 C；C 只能将其作为内部结构占位，不得写入结论。
- 发现 P0 后，暂停所有依赖该结果的下游工作，优先修复并重新复核。
- B 的验证框架和 C 的论文骨架应从项目启动即并行建立，避免串行等待。

## 8. 阶段 7：冻结与结果晋升

只有当复核结论为 `pass` 或 `pass_with_limits`、不存在 P0 问题，且用户明确确认后，才可冻结。冻结包至少包含：

- 最终代码、规范化复现入口、输入和输出哈希；
- 数据、图表、解释性 PDF 和验证报告；
- 假设、限制、适用范围和遗留风险；
- `progress`、`stage`、`review`、Evidence ID 和源 commit。

冻结后写入 `07_frozen/`；只有冻结内容可以晋升到 `08_results/`，并作为 C 正式写作的唯一核心结果来源。

## 9. 阶段 8：C 的论文工作

### 可提前开展

- 背景、问题重述、符号说明和文献综述；
- 方法章节框架、图表样式和论文结构；
- 优缺点、推广方向和局限性框架。

### 必须等待冻结后开展

- 填入正式数字和最终图表；
- 解释模型结论、敏感性和稳健性结果；
- 撰写摘要、结论和量化比较；
- 核对正文、表格、图、附录、引用、单位、精度和强结论；
- 执行提交检查。

## 10. GitHub 文件交接与最小检查

- 每个角色只暂存本次任务的精确路径，使用根规则要求的角色标记、提交格式和远程快照比对流程。
- A、B 的交接以固定 commit 为界；B 的验证产物应独立保存并可追溯到 A 的源 commit。
- 图表必须由已保存的 CSV/JSON 生成；绘图程序不得重新拟合模型或改写正式数字。
- 需要推送时，先比对目标远程分支；发现同一路径或相邻接口存在远程变化时，展示差异与时间戳并等待决定。
- 每次有效修改在提交前至少执行 `git diff --check` 和当前目录声明的验证入口。

## 11. 最终目标

A 输出可验证的候选模型、B 提前建立验证并独立增强、C 提前搭建论文表达；只有经过证据闭环和用户确认的结果才进入冻结、结果层和正式论文。

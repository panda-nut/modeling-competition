# 论文参考结果

本目录只接收从 `07_frozen/` 晋升的稳定参考包。论文写作人员优先阅读 `results-index.md`，再按问题查看结果摘要、数据、图表和演示稿。

禁止：

- 从未冻结 CSV 中挑选数字；
- 在本目录重新拟合模型或重新计算核心结果；
- 手工修正结果而不回到冻结流程；
- 用草稿状态结果填充正式结论。

每次晋升必须生成 `source-manifest.md`，记录源冻结修订、源 commit、机器、时间和文件哈希。

## 2026-08-01 直接写入例外

本批 Q1--Q5 结果依据用户明确授权直接写入本目录，未经过通常的 `07_frozen` 晋升路径。
每问均保留 `source-manifest.md/json`，记录直接写入例外、源 commit、证据编号、限制和
逐文件 SHA-256。该例外不自动延续到后续结果更新。

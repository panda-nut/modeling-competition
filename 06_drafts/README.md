# 演示性论文初稿

本目录保存每问用于讨论的 TEX/PDF 和说明笔记。

- `q1/`—`q5/`：逐问演示稿；
- `notes/`：模型和验证说明；
- 草稿可以引用工作数据和工作图，但必须标记证据边界；
- 草稿不等于 `frozen`，不能直接作为 `08_results/` 的正式来源；
- 只编辑 TEX，PDF 由 MiKTeX/XeLaTeX 重新生成；
- LaTeX 辅助文件不得保存在本目录。

# Paper workspace

`main.tex` is maintained by [C]. [A] and [B] contribute only through their paper-notes directories. Formal values come solely from `08_results/final-results.md`; figures originate in `06_figures/`. Compile with `xelatex main.tex` from this directory.

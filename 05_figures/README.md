# 工作图表

本目录存放工作阶段图表。每张图必须由 `04_code/q*/plotting/` 中的独立程序读取 `03_data/processed/` 的 CSV/JSON 生成。

命名格式：

```text
fig_q3_01_pareto_front.pdf
fig_q3_01_pareto_front.png
```

各问 `README.md` 记录图名、输入数据、生成程序、命令、支持的判断和是否被选入冻结快照。工作图不因外观完成而自动成为正式图。
